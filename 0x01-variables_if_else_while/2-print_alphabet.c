#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - to print random with printf
 *
 * Return: Always 0.
**/

int main(void)
{
	int lwr;

	for (lwr = 'a'; lwr <= 'z'; lwr++)
		putchar(lwr);

	return (0);
}
