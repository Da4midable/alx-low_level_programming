#include "main.h"

/**
 * _isalpha - displays true or false value for lowercase letters
 * @c: the character to be checked
 * Return: 0
 */

int _isalpha(int c)
{

	if (c > 65 && c < 123)
{
	return (1);
}

	return (0);

}
