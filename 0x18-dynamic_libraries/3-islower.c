#include "main.h"

/**
 * _islower - displays true or false value for lowercase letters
 * @c: the character to be checked
 * Return: int c
 */

int _islower(int c)
{

	if (c > 96 && c < 123)
{
	return (1);
}

	return (0);

}
