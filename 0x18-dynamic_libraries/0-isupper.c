#include "main.h"

/**
* _isupper - displays true or false value for lowercase letters
* @c: the character to be checked
* Return: int c
*/

int _isupper(int c)
{

if (c > 64 && c < 91)
{
return (1);
}

return (0);

}
